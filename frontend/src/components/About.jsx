import React, { useContext, useEffect} from 'react'
import NoteContext from '../context/notes/NoteContext'

const About = () => {

  return (
    <div>
      this is about {a.state.name} and he reads in class {a.state.class}
    </div>
  )
}

export default About
